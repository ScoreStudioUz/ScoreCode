{
  "name": "scoreedit",
  "version": "0.1.0",
  "description": "ScoreEdit — lёgkiy redaktor koda",
  "scripts": {
    "dev":   "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  }
}
